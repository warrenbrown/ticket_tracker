<header>
  <h3>New Comment</h3>
</header>

<%= simple_form_for [ ticket, comment ] do |f| %>
  <%= f.input :text %>
  <%= f.submit class: 'btn btn-primary' %>
  <% if policy(ticket).change_state? %>
    <%= f.association :state %>
  <% end %>
<% end %>
